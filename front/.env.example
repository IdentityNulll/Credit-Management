# Only needed for a production build, where the API lives on another host.
# In dev, leave this unset — Vite proxies /api to http://localhost:5555.
# VITE_API_URL=https://your-api-host.example.com/api
